package com.brightly;

import com.brightly.entity.BookEntity;

import java.util.ArrayList;
import java.util.stream.Collectors;

public class TempDp {
    public static ArrayList<BookEntity> bookList=new ArrayList<>();


    private void convertBookEntityToBook(BookEntity entity)
    {
//        Book book=new Book();
//        book.setBookId(book.getBookId());
//        entity.setBookName(book.getBookName());
//        entity.setAuthorName(book.getAuthorName());
//        entity.setPages(book.getPages());
//        return entity;
    }
}
